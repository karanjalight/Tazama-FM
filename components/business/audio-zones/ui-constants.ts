import type { StatColor } from "@/components/business/stat-tile";

/** Cycled by list position purely for visual variety — not tied to any real data. */
export const ICON_COLORS: StatColor[] = ["violet", "amber", "emerald", "blue", "pink"];
