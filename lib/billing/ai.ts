/**
 * AI premium add-on — client-safe constants (no secrets). The Ksh 80 per-user
 * tier that unlocks the concierge, distinct from the account room plans in
 * plans.ts.
 */
export const AI_PRODUCT = "ai";
export const AI_PREMIUM_KES = 80;
